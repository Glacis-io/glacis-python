name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.9", "3.10", "3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

      - name: Type check
        run: mypy glacis/

      - name: Lint
        run: ruff check glacis/

  verify-wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build wheel
        run: |
          pip install build
          python -m build

      - name: Verify WASM in wheel
        run: |
          unzip -l dist/*.whl | grep -q "s3p_core_wasi.wasm"
          echo "WASM binary found in wheel"

      - name: Test wheel installation
        run: |
          pip install dist/*.whl
          python -c "from glacis import Glacis; print('Import OK')"
          python -c "from glacis.wasm_runtime import get_runtime; print(f'Runtime: {get_runtime()}')"
